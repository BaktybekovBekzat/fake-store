import { SafeAreaView, Text } from "react-native";

const CartScreen = () => {
    return (
        <SafeAreaView>
            <Text>Корзина</Text>
        </SafeAreaView>
    );
};

export default CartScreen;
